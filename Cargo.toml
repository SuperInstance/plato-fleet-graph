[package]
name = "plato-fleet-graph"
version = "0.1.0"
edition = "2021"
description = "Fleet graph engine — adjacency lists, centrality, community detection, shortest paths"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
