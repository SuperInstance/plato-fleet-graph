[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-fleet-graph"
version = "0.1.0"
description = "Fleet dependency graph — crate tracking, impact analysis, layer detection"
license = {text = "MIT"}
requires-python = ">=3.8"
